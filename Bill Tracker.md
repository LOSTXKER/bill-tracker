🚀 Ultimate Master Project Plan: "The Bill Tracker & Mini-ERP" for Anajak & Meelike

Goal: สร้าง Web Application ที่เป็น "Database กลาง" สำหรับโรงงานสกรีน (Anajak) และเว็บปั๊มไลค์ (Meelike) เพื่อจัดการกระแสเงินสด, เอกสารทางบัญชี, และภาษีหัก ณ ที่จ่าย ให้ถูกต้อง 100% พร้อมรองรับการขยายตัวสู่ระบบ Custom ERP ในอนาคต

🛠 Tech Stack (High Performance & Scalable)

Frontend/Backend: Next.js (App Router)

Database: PostgreSQL (via Supabase)

ORM: Prisma (เพื่อ Type-safety ที่แข็งแกร่ง)

Storage: Supabase Storage (เก็บรูปสลิปโอนเงิน, ใบกำกับภาษี, สำเนาบิลขาย, ใบ 50 ทวิ)

Auth: NextAuth.js หรือ Supabase Auth

Notification: LINE Notify API (แจ้งเตือนเข้ากลุ่มไลน์ Real-time)

Phase 1: Database Design (Prisma Schema)

ออกแบบ Schema ให้รองรับทั้งการซื้อ (Expense) และการขาย (Income) พร้อมฟิลด์สำหรับจัดการภาษีมูลค่าเพิ่ม (VAT) และภาษีหัก ณ ที่จ่าย (WHT) อย่างละเอียด

// บริษัทในเครือ (รองรับการขยายกิจการในอนาคต)
model Company {
  id        String    @id @default(cuid())
  name      String    // "Anajak T-Shirt", "Meelike-th"
  code      String    // "ANJ", "MLK"
  expenses  Expense[]
  incomes   Income[]
}

// --------------------------------------------------------
// ฝั่งรายจ่าย (Buying & Expenses) - เราจ่ายเงินออก
// --------------------------------------------------------
model Expense {
  id          String   @id @default(cuid())
  companyId   String
  company     Company  @relation(fields: [companyId], references: [id])
  
  // ข้อมูลตัวเลข
  amount      Decimal  @db.Decimal(10, 2) // ยอดเต็มก่อน VAT (Cost Base)
  vatRate     Int      @default(7)        // 7% หรือ 0%
  vatAmount   Decimal? @db.Decimal(10, 2) // ยอด VAT (ถ้ามี)
  description String?  // เช่น "ค่าหมึก DTF", "ค่าจ้างฟรีแลนซ์"
  category    String?  // "Material", "Utility", "Marketing", "Salary"
  
  // เรื่องหัก ณ ที่จ่าย (เราหักเขา) - หน้าที่: ต้องออกใบ 50 ทวิ ให้เขา
  isWht       Boolean  @default(false)    // มีการหัก ณ ที่จ่ายไหม?
  whtRate     Decimal? @db.Decimal(5, 2)  // หักกี่ % (1, 3, 5)
  whtAmount   Decimal? @db.Decimal(10, 2) // ยอดเงินที่หักไว้ (ต้องนำส่งสรรพากร)
  netPaid     Decimal  @db.Decimal(10, 2) // ยอดโอนจริง (Amount + VAT - WHT)
  
  // หลักฐานภาพถ่าย (Evidence)
  slipUrl       String?  // รูปสลิปโอนเงิน (หลักฐานการจ่าย)
  taxInvoiceUrl String?  // รูปใบกำกับภาษี/ใบเสร็จตัวจริง (หลักฐานทางภาษีซื้อ)
  whtCertUrl    String?  // รูปสำเนาใบ 50 ทวิ ที่เราเขียนให้เขา (หลักฐานการหักภาษี)
  
  billDate    DateTime @default(now()) // วันที่จ่ายเงินจริง
  
  // สถานะเอกสาร (Tracking Status)
  status      ExpenseDocStatus @default(PENDING_PHYSICAL)
  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

enum ExpenseDocStatus {
  WAITING_FOR_DOC   // 🟠 จ่ายเงินแล้ว แต่ร้านยังไม่ส่งใบเสร็จมา (ต้องตามทวง!)
  PENDING_PHYSICAL  // 🔴 ได้ใบเสร็จแล้ว ถ่ายรูปแล้ว แต่ตัวจริงยังอยู่ที่ตัว (ในรถ/กระเป๋า)
  READY_TO_SEND     // 🟡 รวบรวมใส่ซอง/กล่องแล้ว (รอส่งบัญชี)
  SENT_TO_ACCOUNT   // 🟢 ส่งให้บัญชี/สำนักงานบัญชีเรียบร้อยแล้ว
}

// --------------------------------------------------------
// ฝั่งรายรับ (Selling & Revenue) - เราได้รับเงิน
// --------------------------------------------------------
model Income {
  id          String   @id @default(cuid())
  companyId   String
  company     Company  @relation(fields: [companyId], references: [id])
  
  // ข้อมูลตัวเลข
  amount      Decimal  @db.Decimal(10, 2) // ยอดเต็มก่อน VAT (Revenue Base)
  vatRate     Int      @default(0)        // ปกติ Anajak อาจจะจด VAT แต่ Meelike อาจจะไม่
  source      String?  // "ลูกค้า K.Somchai", "Daily Sales Meelike"
  
  // เรื่องโดนหัก ณ ที่จ่าย (เขาหักเรา) - หน้าที่: ต้องทวงใบ 50 ทวิ จากเขา
  isWhtDeducted Boolean @default(false)   // โดนลูกค้าหักไหม?
  whtRate       Decimal? @db.Decimal(5, 2) // โดนหักกี่ %
  whtAmount     Decimal? @db.Decimal(10, 2) // ยอดที่โดนหักไป (เครดิตภาษีของเรา)
  netReceived   Decimal  @db.Decimal(10, 2) // ยอดรับจริงเข้าบัญชี
  
  // หลักฐานภาพถ่าย (Evidence)
  customerSlipUrl String?  // รูปสลิปที่ลูกค้าโอนมา (กันลูกค้ามั่วสลิปปลอม)
  myBillCopyUrl   String?  // รูปสำเนาบิลเงินสด/ใบกำกับที่เราเขียนให้ลูกค้า (ส่งบัญชีขาย)
  whtCertUrl      String?  // รูปใบ 50 ทวิ ตัวจริงที่ได้รับจากลูกค้า (สำคัญมาก! ใช้ลดภาษี)
  
  receiveDate DateTime @default(now())
  
  // สถานะเอกสาร
  status      IncomeDocStatus @default(PENDING_COPY_SEND)
  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

enum IncomeDocStatus {
  NO_DOC_REQUIRED     // ⚪ รับเงินแล้ว ไม่ต้องทำเอกสาร (เช่น ยอดเติมเงินรายย่อย)
  WAITING_ISSUE       // 🟠 รับเงินแล้ว แต่ยังไม่ได้เขียน/พิมพ์บิลให้ลูกค้า (ต้องรีบทำ)
  WAITING_WHT_CERT    // 🟠 ออกบิลแล้ว แต่รอลูกค้าส่งใบ 50 ทวิ มาให้ (เคสลูกค้าบริษัท)
  PENDING_COPY_SEND   // 🔴 เอกสารครบ (สำเนาบิล + ใบ 50 ทวิ) รอส่งบัญชี
  SENT_COPY           // 🟢 ส่งปึกสำเนาให้บัญชีแล้ว
}


Phase 2: Core Features (ฟีเจอร์หลักในเวอร์ชันแรก)

1. หน้า Landing & Selector (Mobile First)

เมื่อเปิดเว็บปุ๊บ ต้องเจอ ปุ่มขนาดยักษ์ 2 ปุ่มทันที (ลดเวลาคิด):

🟦 Anajak T-Shirt

🟧 Meelike-th

2. หน้า "Quick Capture" (แยก Tab รับ-จ่าย และระบบคำนวณภาษี)

เมื่อเลือกบริษัทแล้ว จะมี Toggle Tab ด้านบน: [ 💸 จ่ายเงิน ] vs [ 💰 รับเงิน ]

Mode A: ฝั่งจ่ายเงิน (Expense)

Amount Input: ช่องกรอกตัวเลขใหญ่ๆ

Description: พิมพ์สั้นๆ หรือเลือก Tag (ค่าหมึก, ค่าขนส่ง, ค่าแอด, ค่าจ้าง)

Tax Options (ตัวช่วยคำนวณ):

VAT: ปุ่ม Toggle [No VAT] / [VAT 7%] (กดแล้วคำนวณยอดรวมให้ทันที)

WHT (หัก ณ ที่จ่าย): Checkbox ⬜ หัก ณ ที่จ่าย?

ถ้าติ๊ก ✅ -> จะมีปุ่มให้เลือก % (1% ขนส่ง / 3% บริการ / 5% ค่าเช่า)

Display: แสดงยอดที่ต้องโอนจริง (Net Paid) ทันที

Evidence Upload: ปุ่มถ่ายรูปสลิป, ใบเสร็จ, และ สำเนา 50 ทวิ (ถ้ามีการหัก)

Status Selector:

🔘 ได้บิลครบแล้ว -> (Save เป็น PENDING_PHYSICAL)

🔘 ร้านส่งตามมา -> (Save เป็น WAITING_FOR_DOC - แจ้งเตือนให้ตามของ)

Mode B: ฝั่งรับเงิน (Income)

Amount Input: ยอดเงินค่าสินค้า/บริการ

WHT Option (โดนหัก): Checkbox ⬜ โดนหัก ณ ที่จ่าย?

ถ้าติ๊ก ✅ -> ใส่ % ที่โดนหัก -> ระบบแสดงยอดที่ควรเข้าบัญชี

Status Question: "ได้ใบ 50 ทวิ มาหรือยัง?"

🔘 ได้มาแล้ว -> (บังคับถ่ายรูป -> Status ปกติ)

🔘 ลูกค้าส่งตามมา -> (Save เป็น WAITING_WHT_CERT - แจ้งเตือนให้ทวง)

Evidence Upload: ถ่ายสลิปลูกค้า + ถ่ายสำเนาบิลขาย

3. ระบบแจ้งเตือน (LINE Notify API)

ยิงข้อความเข้ากลุ่มไลน์ทันทีที่มีการบันทึก พร้อมรายละเอียดภาษี

Expense: [Anajak] 💸 จ่ายค่าจ้างกราฟิก: 5,000 (หัก 3% = 150) - โอนจริง 4,850

Income: [Anajak] 💰 รับค่าสกรีนจากบ.ยักษ์ใหญ่: 20,000 (โดนหัก 3%) - 🟠 รอใบ 50 ทวิ

4. หน้า Dashboard (The Operations Center)

หน้ารวมรายการที่แบ่งตามความเร่งด่วน (Priority)

Zone 1: ด่วน! ต้องจัดการ (Action Required) 🟠

รายการ WAITING_FOR_DOC (จ่ายแล้ว บิลยังไม่มา): แสดงจำนวนวันที่รอ

รายการ WAITING_WHT_CERT (รับเงินแล้ว ใบหักภาษียังไม่มา): สำคัญมาก ห้ามลืม

รายการ WAITING_ISSUE (รับเงินแล้ว ลืมเขียนบิล)

Zone 2: รอส่งบัญชี (Ready to Batch) 🔴

รายการ PENDING_PHYSICAL (บิลซื้อตัวจริงในมือ)

รายการ PENDING_COPY_SEND (สำเนาบิลขาย + ใบ 50 ทวิ ในมือ)

Action Button: ปุ่ม "Mark as Sent (ใส่ซองแล้ว)" กดทีเดียวเปลี่ยนสถานะทุกรายการเป็น 🟢

Phase 3: Workflow การใช้งานจริง (User Journey)

Scenario 1: จ้างฟรีแลนซ์ออกแบบลาย (Expense - เราหักเขา)

ตกลงค่าจ้าง 1,000 บาท (กฎหมายบอกต้องหัก 3% เพราะเป็นค่าบริการ)

ในแอป: ใส่ยอด 1,000 -> ติ๊ก หัก ณ ที่จ่าย -> เลือก 3%

แอปคำนวณให้: ยอดหัก 30 บาท, ยอดโอน 970 บาท

คุณโอน 970 บาทให้ฟรีแลนซ์

เขียนใบ 50 ทวิ ให้ฟรีแลนซ์ -> ถ่ายรูปสำเนาเก็บลงแอป -> ถ่ายสลิปโอน

กด Save -> จบงาน (ข้อมูลครบ ส่งบัญชีได้เลย)

Scenario 2: รับงานบริษัท (Income - เขาหักเรา)

ส่งงานสกรีน ยอด 10,700 บาท (รวม VAT) -> ลูกค้าโอนมาแค่ 10,400 บาท (เพราะหัก 3% ของยอดก่อน VAT)

ในแอป: ใส่ยอด 10,000 -> VAT 7% -> ติ๊ก โดนหัก ณ ที่จ่าย 3%

เคสลูกค้ายังไม่ให้ใบ: เลือก "ลูกค้าส่งตามมา"

กด Save -> สถานะเป็น 🟠 WAITING_WHT_CERT

(สิ้นเดือน) เปิด Dashboard เจอรายการนี้ค้างอยู่ -> โทรทวงลูกค้า "พี่ครับ ขอใบ 50 ทวิ ด้วยครับ ไม่งั้นผมเสียสิทธิ์ภาษี"

Scenario 3: สั่งของออนไลน์ บิลตามมาทีหลัง (Expense)

โอนเงินค่าหมึก 12,000 บาท

เปิดเว็บ -> เลือก Anajak -> Tab "จ่ายเงิน"

ถ่ายรูปสลิปโอนเงิน -> กดเลือก "ร้านส่งตามมา"

กด Save -> สถานะเป็น 🟠 WAITING_FOR_DOC

(3 วันต่อมา) ไปรษณีย์มาส่งซองเอกสาร

เปิด Dashboard -> จิ้มรายการที่ค้างอยู่ -> ถ่ายรูปใบกำกับภาษีตัวจริงทับลงไป -> Save

สถานะเปลี่ยนเป็น 🔴 PENDING_PHYSICAL -> เอาเอกสารใส่แฟ้ม

Phase 4: Future Growth (การเชื่อมต่อภายนอก)

Excel Export: สร้างปุ่ม Export ข้อมูลแยก Column (วันที่, เลขที่บิล, ชื่อร้าน, ยอดเงิน, VAT, WHT) เพื่อส่งให้บัญชีเอาไป Import เข้า Express/PEAK ได้เลย ไม่ต้องคีย์มือ

PEAK API Integration: ในอนาคตเชื่อม API เพื่อยิงรายการ Expense เข้าไปเป็น "Draft" ใน PEAK อัตโนมัติ

Phase 5: Evolution Path to "Custom ERP" (Level 3) ⭐

นี่คือแผนระยะยาวที่จะเปลี่ยนแอปนี้จาก "สมุดจด" ให้กลายเป็น "ระบบปฏิบัติการ (OS)" ของธุรกิจคุณ โดยเจาะลึกตามธรรมชาติของแต่ละธุรกิจ

Module A: Anajak T-Shirt (Factory Operations)

เน้นการจัดการ "สินค้าที่จับต้องได้" และ "กระบวนการผลิต"

Job Order System (ระบบใบสั่งงาน):

เมื่อมี Income (รับมัดจำ) -> ระบบสร้าง Job Ticket อัตโนมัติ

ระบุรายละเอียด: จำนวนเสื้อ, ไซส์ (S/M/L/XL), สีเสื้อ, ตำแหน่งสกรีน (อกซ้าย/หลัง), ไฟล์ลายสกรีน

Action: Print ใบงานออกมาแปะถุงเสื้อ ให้ลูกน้องทำงานตามใบสั่ง ลดความผิดพลาด

Inventory & Stock (สต็อกเสื้อยืด):

ผูก Job Order กับสต็อก: รับงาน 100 ตัว -> ตัดสต็อกเสื้อดำ ไซส์ L 100 ตัวทันที

Low Stock Alert: แจ้งเตือนทาง LINE เมื่อเสื้อไซส์ยอดฮิต (L, XL) เหลือต่ำกว่าเกณฑ์ (Min Stock) จะได้สั่งของทัน

Costing Calculator (สูตรลับต้นทุนหมึก):

สร้างสูตรคำนวณ: (กว้าง x ยาว ของลาย) x (อัตราสิ้นเปลืองหมึก DTF) = ต้นทุนหมึกต่อตัว

นำไปเทียบกับราคาขาย เพื่อดู กำไรขั้นต้น (Gross Margin) ต่อออเดอร์จริงๆ

Module B: Meelike-th (Digital Operations)

เน้นการจัดการ "ธุรกรรมจำนวนมาก" และ "API Automation"

Automated Revenue Sync (ไม่ต้องกรอกมือ):

เขียน Cron Job (Script อัตโนมัติ) ไปดึงยอดเติมเงินรายวันจาก Database ของ Meelike

สรุปยอดรายวัน: "วันที่ 1 ม.ค. ยอดเติมเงินรวม 50,000 บาท" -> สร้าง Income 1 รายการลงในแอปนี้ให้อัตโนมัติ

Provider Balance Tracker (กันเงินหมด):

ติดตามยอดเงินคงเหลือในเว็บ Provider ต่างประเทศ (ต้นทุนไลค์)

Alert: แจ้งเตือนเมื่อเงินใน Wallet Provider ใกล้หมด เพื่อกันงานสะดุด

Real-time Profit Monitoring:

ดึงยอดขาย (Revenue) จาก Meelike DB

ดึงยอดต้นทุน (Cost) จาก Provider API

แสดงกราฟกำไรรายวัน (Daily Profit) บน Dashboard เพื่อให้เห็นสุขภาพธุรกิจแบบ Real-time